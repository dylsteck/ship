# Cloudflare Worker Local Development Secrets
# Copy this file to .dev.vars and fill in actual values
# .dev.vars is gitignored and used by wrangler dev

# Anthropic API Key (for agent operations)
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# E2B API Key (for sandbox provisioning)
# Get from: https://e2b.dev/dashboard -> Settings -> API Keys
E2B_API_KEY=e2b_...

# Internal API Secret (for authentication between web and API services)
# Generate with: openssl rand -hex 32
API_SECRET=your-api-secret-here

# OpenAI API Key (optional, for Codex agent)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Cursor API Key (optional, for Cursor agent)
CURSOR_API_KEY=

# Allowed CORS Origins (comma-separated)
# In production, set via: wrangler secret put ALLOWED_ORIGINS --env production
ALLOWED_ORIGINS=http://localhost:3000

# Login restriction (optional) - when true, only ALLOWED_USER_ID can log in
# Default: off. Set to "true" to enable.
LOGIN_RESTRICTED_TO_SINGLE_USER=false
# Your app user ID from the users table (required when restricted)
# Find yours: wrangler d1 execute ship-db --local --command "SELECT id FROM users LIMIT 1"
ALLOWED_USER_ID=
