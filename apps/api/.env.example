# Convex
CONVEX_URL=https://your-deployment.convex.cloud

# Anthropic (for OpenCode)
ANTHROPIC_API_KEY=your-anthropic-api-key

# API Key for Convex HTTP actions (shared secret between API and Convex)
# Generate with: openssl rand -hex 32
API_KEY=your-api-key-secret

# GitHub App https://github.com/settings/apps/new
GITHUB_APP_ID=123456
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
GITHUB_APP_INSTALLATION_ID=12345678

# Vercel (for Sandbox)
# Option 1: OIDC Token (recommended, auto-managed in production)
# For local dev: run `vercel env pull` to get OIDC token
# VERCEL_OIDC_TOKEN=your-oidc-token

# Option 2: Access Token (alternative, no env pull needed)
# Get from: Team Settings (Team ID), Project Settings (Project ID), Account Settings → Tokens
# VERCEL_TEAM_ID=team_xxxxx
# VERCEL_PROJECT_ID=prj_xxxxx
# VERCEL_TOKEN=your-vercel-access-token
