/** @type {import('next').NextConfig} */
const nextConfig = {
  // Production API URL is set via NEXT_PUBLIC_API_URL env var
  // Defaults to http://localhost:3000 for local development
  // Production: https://api.ship.dylansteck.com
};

export default nextConfig;
