/**
 * Shared TypeScript types for Ship monorepo
 */

// User types
export type { User, CreateUserInput, UserDTO } from './user'

// Authentication types
export type { Session, Account } from './auth'
